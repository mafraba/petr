= s3_uploader_form 	callback_url: "#{profile_path(@profile)}",
          callback_method: "PATCH",
          max_file_size: 5.megabytes,
          acl: 'private',
					id: "s3-uploader" do
  = file_field_tag :file, multiple: false

%script#template-upload{type: "text/x-tmpl"}
  .upload{id: 'file-{%=o.unique_id%}'}
    {%=o.name%}
    .progress
      .bar{style: "width: 0%"}

#uploads
  %header
    Uploading...
  #progress-meters

