// Update logo picture shown
$('#logo-edit').attr("src", '<%= @profile.logo.url(:thumbnail) %>')
// Close modal
$('#uploads').modal('hide');
// Clear progress bars in case the modal is shown again
$('#progress-meters').html('');
